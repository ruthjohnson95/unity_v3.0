


def gibbs_sampler(z, N, ld_half_file, f, its, DP_flag=True, profile_flag=False, H_snp=None):
    # TODO: separate heavy pre-computations in a separate script and as backup in main script
    return